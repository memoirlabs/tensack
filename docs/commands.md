# Tensack Commands

Status: placeholder.

This document will define the CLI command surface, exit codes, output stability rules, and scripting guarantees.

## Initial Surface

- `tensack --version` (or `tensack -V`) - print the CLI version.
- `tensack help` (or `-h`, `--help`) - print usage.
