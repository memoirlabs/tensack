# Tensack Admin UI

Status: planned.

This app will provide a local interface for viewing and inspecting a Tensack database.

It is intentionally not wired to a frontend framework yet. The first real implementation should be chosen after the CLI, store API, and file format boundaries are clearer.
